specVersion: "v1beta"
serviceId: "skillbench"
location: "asia-south1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "quip-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
